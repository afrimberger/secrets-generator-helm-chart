image:
  repository: dtzar/helm-kubectl
  tag: "3.14"
  pullPolicy: IfNotPresent

serviceAccount:
  name: secrets-generator

# set to true if this is the only Helm Chart in the ArgoCD application.
# It creates a dummy configMap so that there is one resource to sync.
createDummyResource: false


secrets: []
#  - name: my-example-password
#    data:
#      password: "{{ randAlphaNum 32 }}"
#  - name: my-private-key
#    data:
#      SIGNING_KEY: |
#        {{ genPrivateKey "ec" }}
